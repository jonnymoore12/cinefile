<%= "Welcome to #{@user.username}'s Cinefile" %>
<%#= button_to "Add Film", edit_user_cinefile_list_film_path(@user.id, @cinefile.id, @list_film.id), method: :get %>

<%= form_for @list_film do |f|%>
  <%= f.select(:film_id, [['Jaws', 1]) %>
  <%= f.submit %>
<% end %>
