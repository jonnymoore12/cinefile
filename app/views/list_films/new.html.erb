

<%= form_for @filmtemp do |f| %>
  <% @search = Tmdb::Search.new %>
  <% @search.resource('movie') %>
  <% @search.query(params[:search]) %>
  <% @results = @search.fetch %>
  <% #@titles = %>


  <%#= select_tag "films", options_from_collection_for_select(results, "imdb_id", "title"), prompt: "Select something" %>
  <%#= form_for @film do |f| %>
    <%= select_tag "films", options_from_collection_for_select(@results, 'to_s', to_s), prompt: "Select something" %>
    <%= f.submit %>
  <% end %>


  <%#= select_tag "films", options_from_collection_for_select(@films, "imdb_id", "title"), prompt: "Select something" %>
